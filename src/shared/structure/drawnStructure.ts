/**
 * The label a structure drawn on the canvas carries.
 *
 * It is a placeholder rather than a name, so it is never offered as a value to
 * copy.
 */
export const DRAWN_STRUCTURE_LABEL = 'Drawn structure';
